# Testing

This page is for contributors adding or changing framework behavior.

## Expectations

Framework changes should include tests for the behavior they introduce or modify.

Use the existing `src/Tests.*` projects as the starting point for locating relevant test patterns.

## Documentation And Tests

Examples in consumer documentation should preferably come from tested behavior. If documentation describes generated output, tests should cover that generation path.
